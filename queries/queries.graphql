query PAGES {
	pages {
		sections
		slug
	}
}

query PAGE($slug: String!) {
	pages(where: {slug: {_eq: $slug}}) {
		sections
	}
}

query NEWS {
	news: v_news {
		id
		slug
		heading
		picture
		originla_lang
		sections
		settings
	}
}
